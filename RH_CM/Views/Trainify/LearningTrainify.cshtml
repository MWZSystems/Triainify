@model IEnumerable<RH_CM.ViewModels.LearningCourseToDoViewModel>
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@{
    ViewData["Title"] = "Next required courses to do";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var first = Model?.FirstOrDefault();
    var courseGroups = Model?.GroupBy(m => m.CourseName).ToList()
                     ?? new List<IGrouping<string, RH_CM.ViewModels.LearningCourseToDoViewModel>>();
}

<div class="row">
    <div class="col">
        <h1 class="h3 mb-3 text-center">
            <i class="align-middle fas fa-graduation-cap"></i> @ViewData["Title"]
        </h1>
    </div>
</div>

<div class="row">
    <div class="col-sm-12">
        <div class="card">

            <div class="card-header">
                @if (first != null)
                {
                    <div class="mb-3 text-center d-flex flex-wrap justify-content-center gap-3">
                        <div class="p-3 fw-bold rounded fs-3">
                            Control #: @first.CONTROL_NUMBER
                        </div>
                        <div class="p-3 fw-bold rounded fs-3">
                            Full name: @first.FullName
                        </div>
                        <div class="p-3 fw-bold rounded fs-3">
                            Position: @first.NAME_POSITION_ENGLISH
                        </div>
                        <div class="p-3 fw-bold rounded fs-3">
                            Courses: @courseGroups.Count
                        </div>
                    </div>
                }
            </div>

            <div class="card-body">

                @if (Model != null && Model.Any())
                {
                    <table id="datatables-learning" class="table table-striped table-hover" style="width:100%">
                        <thead class="table-primary">
                            <tr>
                                <th># Assign.</th>
                                <th># Course</th>
                                <th>Course</th>
                                <th># LevelId</th>   @* 👈 nuevo: FK_RequiredCourseLevels *@
                                <th>Level</th>
                                <th>Delivery</th>
                                <th>Validity (days)</th>
                                <th>Status</th>
                                <th>Last update</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.PK_CourseAssignment</td>
                                    <td>@item.FK_Course</td>
                                    <td>@item.CourseName</td>
                                    <td>@item.FK_RequiredCourseLevels</td> @* 👈 nuevo *@
                                    <td>@item.CourseLevel</td>
                                    <td>@item.DeliveryMode</td>
                                    <td>@(item.CourseValidityDays?.ToString() ?? "—")</td>
                                    <td>@item.CourseStatus</td>
                                    <td>@(item.LastUpdateDate?.ToString("yyyy-MM-dd HH:mm") ?? "—")</td>
                                    <td>
                                        <div class="text-center d-flex gap-1">
                                            <!-- Iniciar: abre en nueva pestaña → StartCourse(courseId, levelId) -->
                                            <a class="btn btn-primary"
                                               asp-controller="Catalog"
                                               asp-action="StartCourse"
                                               asp-route-courseId="@item.FK_Course"
                                               asp-route-levelId="@item.FK_RequiredCourseLevels"
                                               title="Start"
                                               target="_blank" rel="noopener">
                                                <i class="fas fa-play"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <p>No records found</p>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")

    <script>
        $(document).ready(function () {
            $('#datatables-learning').DataTable({
                responsive: true,
                // Columnas: 0=#Assign, 1=#Course, 2=Course, 3=#LevelId, 4=Level, ...
                order: [[2, 'asc'], [4, 'asc']]
            });

            // Notificaciones (Notyf) con comillas seguras
            if ('@TempData["SuccessMessage"]' !== '') {
                window.notyf?.open?.({
                    type: 'success',
                    message: '@TempData["SuccessMessage"]',
                    ripple: true,
                    dismissible: true,
                    position: { x: 'center', y: 'top' },
                    duration: 5000
                });
            }
            if ('@TempData["ErrorMessage"]' !== '') {
                window.notyf?.open?.({
                    type: 'error',
                    message: '@TempData["ErrorMessage"]',
                    ripple: true,
                    dismissible: true,
                    position: { x: 'center', y: 'top' },
                    duration: 5000
                });
            }
        });
    </script>
}
